{
    "name": "ptnl-constructor-sdk",
    "version": "0.7.148",
    "description": "Набор инструментов для создания виджетов.",
    "files": [
        "**/*.js",
        "**/*.d.ts"
    ],
    "types": ".",
    "scripts": {
        "build": "npm run clean && tsc",
        "clean": "rimraf ./*.js ./*.d.ts __internal__",
        "prepublishOnly": "npm run build && npm version patch && git push",
        "start": "npm run clean && tsc  --watch --inlineSourceMap",
        "test": "exit 0"
    },
    "dependencies": {
        "logical-not": "^1.0.7"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typedoc": "^0.20.23",
        "typescript": "^4.1.3"
    }
}
